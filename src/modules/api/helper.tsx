import ApiRoutes from '../../../config/api-routes';

// Enum contains all rest methods names
export enum EApiMethods {
	get = 'get',
	post = 'post',
	delete = 'delete',
	patch = 'patch',
}

// Api routes
export const Routes = ApiRoutes;
