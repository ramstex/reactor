export enum EButtonType {
	button = 'button',
	submit = 'submit',
	reset = 'reset',
}

export enum EButtonTheme {
	default = 'default',
	primary = 'primary',
	secondary = 'secondary',
}

export enum EButtonTemplate {
	default = 'default',
	ghost = 'ghost',
	link = 'link',
}
