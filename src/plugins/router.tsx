import {
	Link as LinkLocal,
	BrowserRouter as RouterLocal,
	Routes as RoutesLocal,
	Route as RouteLocal,
	Outlet as OutletLocal
} from 'react-router-dom';

export const Link = LinkLocal;
export const Router = RouterLocal;
export const Routes = RoutesLocal;
export const Route = RouteLocal;

export const Outlet = OutletLocal;
