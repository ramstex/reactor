// export function prepareFormData( data, schema ) {
// 	const fData = new FormData();
//
// 	Object.entries( schema ).forEach( ( [ key, value ] ) => {
// 		!!data[ key ] && fData.append( value, data[ key ] );
// 	} );
// }
